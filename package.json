{
    "name": "perseusx-code-exercise",
    "version": "0.0.0",
    "repository": "git@github.com:franklinesquivel/perseusx-code-exercise.git",
    "author": "Franklin Esquivel <franklin.esquivel@outlook.com>",
    "license": "MIT",
    "scripts": {
        "build": "tsc -p tsconfig.build.json && tsc-alias",
        "build:watch": "tsc -p tsconfig.build.json && (concurrently \"tsc -p tsconfig.build.json -w\" \"tsc-alias -w\")",
        "start": "node ./build/index.js",
        "test": "jest"
    },
    "devDependencies": {
        "@types/jest": "^29.5.1",
        "concurrently": "^8.0.1",
        "jest": "^29.5.0",
        "ts-jest": "^29.1.0",
        "ts-node": "^10.9.1",
        "tsc-alias": "^1.8.6",
        "typescript": "^5.0.4"
    }
}
